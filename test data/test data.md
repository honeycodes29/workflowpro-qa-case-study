# Test Data Strategy

Test data is created dynamically using backend APIs.

Approach:
- Unique project names are generated for each test run
- Tenant-specific users are used for validation
- API-based data creation avoids UI dependency
- Test data can be cleaned up after execution if required
